

export default function Titlebar({label}) {
  return (    
     <div>
     <p className=" text-green-800 text-2xl cursor-pointer hover:text-black">
     <b>{label}</b>
   </p>
   </div>
  )
}
